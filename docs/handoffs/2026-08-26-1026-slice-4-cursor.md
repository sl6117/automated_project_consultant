# Handoff: Phase 1 Slice 4

## Goal

Continue the localhost Automated Project Consultant through Phase 1 while
preserving traceable ledger state, human-owned decisions, offline tests, model
cost/privacy boundaries, and hands-on AI engineering learning.

## Working mode & rules in effect

- Guided-driver mode is active and sticky.
- Claude Code is the primary product-code implementer against approved specs.
  Cursor should teach, inspect evidence, and review decisions without becoming
  a message relay for routine implementation details.
- Use one guided checkpoint at each slice's primary AI boundary. After owner
  teach-back and approval, Claude Code handles mechanical UI, SQL, wiring, and
  tests autonomously within scope.
- Pause only for owner-managed files, new dependencies, material spec
  deviations, irreversible choices, or a second failed attempt requiring a
  re-plan.
- End each slice with one read-only reviewer pass, the full verification suite,
  and a separate green commit.
- Owner-managed files are `CLAUDE.md`, `.claude/settings.json`,
  `.claude/hooks/**`, and `.claude/agents/**`. Do not edit one without explicit
  owner authorization for that file.
- Do not install dependencies, stage, commit, or push without explicit owner
  approval. Never use blanket `git add .` or `git add -A`.
- Tests and CI stay offline. Real consultations remain gitignored.

## Status

Phase 0 and Phase 1 slices 1–3 are complete and committed on `main`. Phase 1 is
roughly halfway through its six implementation slices. The owner is proceeding
with Slice 4 in Claude Code.

Two process/context edits plus this handoff are currently uncommitted:

- `.claude/skills/guided-driver/SKILL.md`
- `docs/learning-roadmap.md`
- `docs/handoffs/2026-08-26-1026-slice-4-cursor.md`

## Done so far

- `dd55f22` — Phase 0 least-privilege Claude Code harness, Next.js scaffold,
  offline tests, and CI.
- `f8bff9c` — Phase 1 slices 1–3: validated SQLite ledger, migrations 001–004,
  statement/concern review, revision provenance, recorded and synthetic model
  clients, atomic extraction plus first-question persistence, safe action
  errors, and offline browser coverage.
- Last verified baseline:
  - `npm run typecheck` passed.
  - `npm run lint` passed.
  - `npm test` passed: 11 files, 47 tests.
  - `npm run build` passed without recorded-client tracing warnings.
  - `npm run test:browser` passed: 11 Chromium tests.
- Read-only code review findings were fixed before `f8bff9c`.
- Learning checkpoints completed for Zod versus transaction atomicity,
  provenance, and stub versus recorded model boundaries.
- The learning roadmap now reserves one bounded, read-only Claude Code dynamic
  workflow lab for Phase 3; persistent ultracode remains off.

## Next action (start here)

First inspect `git status` and the active Claude Code result. Keep the three
context-only files above separate from Slice 4 product work; obtain explicit
owner approval before committing them.

Then continue Slice 4 sequentially in the current checkout. Its one guided
checkpoint is the structured coach-recommendation boundary: define what the
model may recommend, how confidence and evidence-that-would-change-it are
represented, how coach notes remain optional, and why unpromoted coaching must
stay out of future exported specifications. After owner teach-back and approval,
Claude Code may implement the mechanical ledger, fixture, UI, and tests without
routine relaying through Cursor.

## Roadmap / remaining

- Phase 1 Slice 4: optional coach panel backed by separate `coach_notes`;
  unpromoted coaching excluded from future compiler output.
- Phase 1 Slice 5: ledger-only artifact compiler, immutable versions, and
  Markdown browser downloads.
- Phase 1 Slice 6: owner-approved `@anthropic-ai/sdk`, live client behind
  `ModelClient`, prompt/cache construction, token/cost accounting, and explicit
  over-cap confirmation. Tests and CI remain recorded/offline.
- Phase 2: adaptive question selection and stopping.
- Phase 3: synthetic evaluation baseline and bounded dynamic-workflow learning
  lab.
- Phase 4: evidence-gated bounded runtime swarm.
- Phases 5–6: living feedback and read-only brownfield consultation.

## Key files & locations

- Cursor plan:
  `/Users/thomaslee/.cursor/plans/project_feedback_loop_f88bf4b1.plan.md`
  (its todo statuses are stale; do not edit it or treat them as current truth).
- Product contract: `CLAUDE.md`
- Phase 1 spec: `docs/specs/phase-1-greenfield-slice.md`
- Learning cadence: `docs/learning-roadmap.md`
- Claude guided-driver skill: `.claude/skills/guided-driver/SKILL.md`
- Prior Claude handoff:
  `docs/handoffs/2026-08-25-1747-claude-code-phase-1.md`
- Product UI/actions: `src/app/`
- Ledger/database/model boundary: `src/server/`
- Offline fixtures/tests: `tests/`
- Last commit: `f8bff9c Implement the Phase 1 consultation foundation`

## Open questions / gotchas

- Claude Code's sandbox cannot bind local ports in this setup. The owner runs
  `npm run build` and `npm run test:browser` in a normal terminal when required.
- Slice 4 and Slice 5 share the coaching-exclusion/compiler contract; keep them
  sequential rather than forcing worktree parallelism.
- Do not install `@anthropic-ai/sdk` before Slice 6 and explicit owner approval.
- The Phase 1 spec status header is current, but the Slice 2 line in its
  implementation list still says “in progress”; correct that only as a bounded
  documentation update.
- Do not enable ultracode for ordinary product implementation. The first dynamic
  workflow is the bounded Phase 3 read-only learning lab.
- Do not claim live Fable/Sonnet behavior before Slice 6.

## How to resume

Open a new Cursor chat, paste this whole document, then say:

"Continue from this handoff. Next action: inspect the context-only changes and
the current Claude Code Slice 4 checkpoint; preserve the one-checkpoint-per-slice
guided cadence."
