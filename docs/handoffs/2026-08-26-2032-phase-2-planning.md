# Handoff: Begin Phase 2 adaptive consultation

## Goal
Continue the Automated Project Consultant after completing Phase 1. Design and approve a Phase 2 specification for adaptive consultation before writing product code.

## Working mode & rules in effect
- Guided-driver mode is sticky: the user runs commands and owns learning checkpoints; the assistant explains, reviews, and waits unless explicitly asked to edit or execute.
- Preserve the one-checkpoint-per-product-slice cadence documented in `docs/learning-roadmap.md`.
- Product code requires an approved phase specification with testable acceptance criteria.
- Do not edit owner-managed `CLAUDE.md`, `.claude/settings.json`, `.claude/hooks/**`, or `.claude/agents/**` without explicit approval for the named file.
- The ledger is canonical; models only propose; structured output is validated before persistence; coaching remains separate from exports unless promoted.
- Tests and CI must remain offline. Live calls require explicit owner initiation and the default $5 session cap.
- Do not begin runtime delegation or a swarm in Phase 2. The single-consultant evaluation baseline comes first; the bounded read-only multi-agent lab remains Phase 3.

## Status
Phase 1 is complete, verified, and committed on clean `main`. Phase 2 has not been specified or implemented yet.

## Done so far
- Completed the six Phase 1 slices: canonical SQLite ledger and Zod contracts, recorded extraction and approval UI, one-question loop, coach panel with explicit promotion, deterministic immutable artifact exports, and the live Anthropic client.
- Hardened live execution with asynchronous model I/O outside transactions, create-then-settle attempt receipts, integer-microcent accounting, reserved unknown transport spend, exact-request estimation, retryable failed initialization, and explicit over-cap confirmation.
- Added one byte-identical tagged Fable output format shared by next-question and coaching calls while retaining task-specific Zod validation.
- Offline verification passed: typecheck and lint clean; `npm test` passed 18 files / 110 tests; production build passed; Playwright passed 16/16.
- Owner-run live smoke test passed. The first Fable call wrote 2,890 cache tokens; the second read exactly 2,890 and wrote 0. All calls succeeded and total smoke cost was about $0.13.
- The Anthropic API key used for the smoke test was cleared. Never request or expose its value.
- Phase 1 status was closed in the specification and learning roadmap.
- Latest commit: `942d1be Add live model client and close Phase 1`.
- Recent baseline commits:
  - `4f5d006 Add the slice 5 artifact compiler with immutable exports`
  - `701f1a4 Document the guided product-slice cadence`
  - `4bcfb73 Add the slice 4 coach panel with explicit promotion`
  - `f8bff9c Implement the Phase 1 consultation foundation`

## Next action (start here)
Collaboratively draft `docs/specs/phase-2-adaptive-consultation.md`. First inspect the current Phase 1 contracts and Phase 2 roadmap paragraph, then ask the owner the minimum decisions needed to define concern coverage, contradiction detection, candidate-question ranking, visible scoring, and a deterministic stopping rule. Do not implement Phase 2 until the owner approves the specification and acceptance criteria.

## Roadmap / remaining
- Define Phase 2 goals, non-goals, invariants, user-visible flow, ledger additions, model boundaries, offline evaluation fixtures, and acceptance criteria.
- Divide the approved Phase 2 work into small product slices with one meaningful learning checkpoint each.
- Implement each approved slice with offline tests and owner verification.
- Establish the single-consultant evaluation baseline before Phase 3.
- In Phase 3, ask the owner again whether to include both Codex and Claude Code in the bounded orchestration experiment.

## Key files & locations
- Phase 1 contract: `docs/specs/phase-1-greenfield-slice.md`
- Learning sequence and Phase 2 outline: `docs/learning-roadmap.md`
- Project rules: `CLAUDE.md` and `AGENTS.md` (read-only unless explicitly unlocked)
- Canonical schemas: `src/server/ledger/schemas.ts`
- Session and question flow: `src/server/ledger/sessions.ts`, `src/server/model/next-question.ts`
- Prompt and structured-output contracts: `src/server/model/prompt.ts`
- Model attempt and cost boundary: `src/server/model/attempt-runner.ts`, `src/server/ledger/model-attempts.ts`
- Model configuration: `src/server/model/config.ts`
- Existing synthetic fixtures: `tests/fixtures/phase-1/`
- Existing handoff history: `docs/handoffs/`
- No relevant Cursor plan file or active todo list exists for Phase 2.
- Last commit: `942d1be Add live model client and close Phase 1`

## Open questions / gotchas
- Decide whether question ranking is deterministic application logic, Fable structured output, or a bounded hybrid. Do not assume this before owner discussion.
- Define a visible scoring rubric for uncertainty reduction/value of information and how ties are resolved.
- Define contradiction semantics and whether Phase 2 only surfaces contradictions or also proposes supersession; humans must remain the only authority that changes canonical decisions.
- Define “ready enough to stop” as an inspectable rule, not an opaque model judgement.
- Specify the minimum synthetic evaluation set and regression thresholds without moving the full evaluation program out of Phase 3.
- Maintain legacy compatibility with Phase 1 sessions and migrations.
- Do not add cheaper-model routing, runtime agents, shell access, repository editing, or delegation in Phase 2.
- Live SQLite files under `data/` are gitignored and must remain private. No live call is needed to draft or test the Phase 2 specification.
- Commit `942d1be` has not been pushed.

## How to resume
Open a new chat, paste this whole document, then say:
"Continue from this handoff. Next action: collaboratively draft and approve the Phase 2 adaptive-consultation specification; preserve guided-driver mode and do not implement product code yet."
